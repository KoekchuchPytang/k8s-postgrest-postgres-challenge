apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: k8s-desafio
  labels:
    app: postgres
data:
  POSTGRES_DB: "meubanco" 
  PGDATA: "/var/lib/postgresql/data/pgdata" 
  POSTGRES_PORT: "5432"  
